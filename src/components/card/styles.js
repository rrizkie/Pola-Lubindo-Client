import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  media: {
    height: 0,
    paddingTop: "56.25%", // 16:9
  },
  produkTitle: {
    fontWeight: "bold",
    fontSize: "0.8rem",
  },
  beli: {
    backgroundColor: "red",
    color: "#fff",
    fontWeight: "bold",
    fontSize: "0.7rem",
  },
});