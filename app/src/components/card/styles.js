import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  root: {
    maxWidth: 160,
    margin: "0.5rem",
  },
  media: {
    height: 140,
  },
  produkTitle: {
    fontWeight: "bold",
    fontSize: "1rem",
  },
  beli: {
    backgroundColor: "red",
    color: "#fff",
    fontWeight: "bold",
    fontSize: "0.7rem",
    padding: "0.7vh 15vw",
  },
});
